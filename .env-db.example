# Креды для production БД мигрируемого сайта.
# Скопируйте в .env-db и заполните теми же значениями, которые
# использует сайт в htdocs/config.php (production-блок) — чтобы не
# править код после миграции.
# НЕ коммитить .env-db в git!

DB_USER=
DB_PASS=
