<?php
/*
 * Баннер "Перейти на главную сайта" — выводится сверху и снизу каждой
 * страницы Wolf-а (рендерится из page.phtml).  Цель: посетители, попавшие
 * сюда из поисковой выдачи на устаревшие URL, должны очевидно увидеть,
 * что актуальный сайт — это лендинг zorge9.com, и иметь явный путь туда.
 *
 * $z9_banner_position = 'top' | 'bottom' — задаётся вызывающим include'ом.
 */
$position = isset($z9_banner_position) ? $z9_banner_position : 'top';
$is_top = $position === 'top';
$z9_banner_msg = $is_top
    ? 'Вы находитесь на странице с архивной информацией о&nbsp;проекте.'
    : 'Это страница со старыми материалами.';
$z9_banner_cta = 'Открыть актуальный сайт ЖК Зорге 9';
?>
<div class="z9-to-landing-banner z9-to-landing-banner--<?= htmlspecialchars($position) ?>"
     role="region" aria-label="Переход на главный сайт">
    <div class="z9-to-landing-banner__inner">
        <div class="z9-to-landing-banner__text"><?= $z9_banner_msg ?></div>
        <a class="z9-to-landing-banner__cta" href="/"><?= htmlspecialchars($z9_banner_cta) ?> &nbsp;→</a>
    </div>
</div>
<?php if ($is_top): ?>
<style>
.z9-to-landing-banner {
    background: #111;
    color: #f4f4f4;
    border-bottom: 1px solid #c7a55a;
    padding: 14px 24px;
    font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
    font-size: 15px;
    line-height: 1.4;
    z-index: 99999;
    position: relative;
}
.z9-to-landing-banner--bottom {
    border-top: 1px solid #c7a55a;
    border-bottom: 0;
    padding: 20px 24px;
}
.z9-to-landing-banner__inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 14px 24px;
    justify-content: space-between;
}
.z9-to-landing-banner__text { flex: 1 1 auto; min-width: 0; }
.z9-to-landing-banner__cta {
    background: #c7a55a;
    color: #111;
    text-decoration: none;
    padding: 9px 18px;
    border-radius: 4px;
    font-weight: 600;
    font-size: 14px;
    white-space: nowrap;
    transition: background .15s ease;
}
.z9-to-landing-banner__cta:hover { background: #d8b96e; color: #111; }
@media (max-width: 640px) {
    .z9-to-landing-banner { padding: 12px 16px; font-size: 14px; }
    .z9-to-landing-banner__inner { flex-direction: column; align-items: stretch; gap: 10px; }
    .z9-to-landing-banner__cta { text-align: center; }
}
</style>
<?php endif; ?>
