# Тестовый поддомен: сайт временно скрыт от поисковых систем.
#
# ПОДГОТОВЛЕНО:
#  - директива Sitemap (карта сайта для поисковиков);
#  - правило запрета индексации Google на /about/developer.
# Пока действует тестовая блокировка всего сайта (строки [TEST-BLOCK]).
# При выходе на боевой домен удалите ОБЕ строки, помеченные [TEST-BLOCK], —
# сайт станет индексируемым, а Sitemap и запрет /about/developer сохранятся.

Sitemap: https://zorge9.com/sitemap.xml

User-agent: Googlebot
# [TEST-BLOCK] удалить при go-live
Disallow: /
# Постоянное правило: Google не индексирует эту страницу
Disallow: /about/developer

User-agent: *
# [TEST-BLOCK] удалить при go-live
Disallow: /
